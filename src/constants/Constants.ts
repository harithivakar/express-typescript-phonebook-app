export default class Constants{
    public static readonly DOMAIN = "/phonebook";
    public static readonly CONTEXT_PATH = `${Constants.DOMAIN}/api`;
    public static readonly DOCS_PATH = `${Constants.DOMAIN}/api/docs`;
}